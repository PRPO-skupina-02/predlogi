apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: predlogi
  namespace: production
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    kubernetes.io/ingress.class: "gce"
spec:
  tls:
  - hosts:
    - predlogi.prpo.timhrovat.com
    secretName: predlogi-tls
  rules:
  - host: predlogi.prpo.timhrovat.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: predlogi
            port:
              number: 8080
