apiVersion: v1
kind: Service
metadata:
  name: predlogi
  namespace: production
  labels:
    app: predlogi
  annotations:
    cloud.google.com/backend-config: '{"default": "predlogi-backendconfig"}'
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: predlogi
